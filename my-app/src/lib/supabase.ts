// ============================================================
// lib/supabase.ts — Stub File (ไม่ใช้ Supabase แล้ว)
// เหลือไว้เพื่อไม่ให้ import ที่ยังอ้างถึง file นี้ break
// ห้าม import Prisma ตรงนี้! เพราะจะถูก import โดย client components
// ============================================================

// Stub ที่ไม่ throw error ตอน build และไม่ import Prisma (server-only)
export function createClient() {
  return null as any;
}

export function createServerClient() {
  return null as any;
}

export function getSupabaseBrowser() {
  return null as any;
}
