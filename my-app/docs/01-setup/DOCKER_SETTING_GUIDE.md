# 🐳 คู่มือการเตรียมความพร้อมสำหรับ Docker (Docker Setup Guide)

เอกสารนี้รวบรวมรายการสิ่งที่ต้องเตรียมและเวอร์ชันต่างๆ ที่จำเป็นหากต้องการปรับเปลี่ยนโปรเจคนี้ให้รันบน Docker ได้ในอนาคต

---

## 🛠️ 1. สิ่งที่ต้องติดตั้งในเครื่อง (System Requirements)
- **Docker Engine / Docker Desktop**: เวอร์ชัน 20.10.x หรือใหม่กว่า
- **Docker Compose**: เวอร์ชัน 2.x (แนะนำเพื่อความสะดวกในการจัดการ Container)

## 📦 2. สภาพแวดล้อมภายใน Container (Environment)
- **Base Image**: แนะนำใช้ `node:18-alpine` หรือ `node:20-alpine`
  - *เหตุผล*: Alpine Linux มีขนาดเล็กมาก ช่วยให้ Image ของเราไม่หนักเครื่องและโหลดไว
- **Package Manager**: ใช้ `npm` (ตามโปรเจคปัจจุบัน)

## ⚙️ 3. การตั้งค่าที่จำเป็น (Required Configurations)
หากจะใช้ Docker ต้องตรวจสอบจุดเหล่านี้:

### A. Next.js Configuration (`next.config.js`)
ต้องมีการเพิ่ม `output: 'standalone'` เพื่อให้ Next.js สร้างไฟล์ที่จำเป็นสำหรับการรันบน Node.js โดยตรงโดยไม่ต้องพึ่ง `node_modules` ทั้งหมด (ช่วยลดขนาด Image ลงมหาศาล)

### B. ไฟล์ที่ต้องสร้างเพิ่ม (Required Files)
1. **Dockerfile**: เป็น "สูตรอาหาร" บอกวิธีสร้าง Container (ต้องใช้ Multi-stage build เพื่อความปลอดภัยและขนาดที่เล็ก)
2. **.dockerignore**: บอก Docker ว่าไม่ต้องหยิบไฟล์ไหนไปบ้าง (เช่น `node_modules`, `.git`, `.env.local`)
3. **docker-compose.yml**: (ตัวเลือกเสริม) ใช้สำหรับระบุ Port, ชื่อ Container และการเชื่อมโยงไฟล์ Environment Variable

## 🔑 4. ข้อมูลที่ต้องใช้ (Environment Variables)
ต้องเตรียมค่าเหล่านี้เพื่อส่งให้ Docker ตอนรัน:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `NEXT_PUBLIC_APP_URL`

## 🚀 5. รูปแบบการทำงาน (Workflow)
1. **Build Stage**: ติดตั้ง dependencies และทำการ `npm run build`
2. **Runner Stage**: คัดลอกเฉพาะไฟล์ที่ถูก build แล้ว (`.next/standalone`) มาทำงาน
3. **Port**: ระบบภายในรันที่ Port `3000` (สามารถ Map ออกมาเป็น Port อื่นได้ตามต้องการ)

---
*หมายเหตุ: ปัจจุบันโปรเจคนี้ยังไม่มีไฟล์เหล่านี้ติดตั้งอยู่ หากจะเริ่มใช้งานในอนาคต สามารถแจ้งให้ผมสร้างไฟล์ตามรายการข้างต้นได้ทันทีครับ*
