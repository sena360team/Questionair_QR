# 📋 Card Based Form Guide

## ปัญหา: แบ่ง Section อย่างไร?

ระบบปัจจุบันมี **`section` field type** อยู่แล้ว ใช้แบ่งกลุ่มคำถามได้

---

## 🔧 โครงสร้าง Field Types ที่มี

### Layout Fields (ใช้จัดโครงสร้าง)
| Type | ใช้ทำอะไร | หน้าตา |
|------|----------|--------|
| `heading` | หัวข้อใหญ่ | ตัวอักษรหนาๆ |
| `section` | **แบ่งกลุ่ม/Card** | ชื่อ Section |
| `info_box` | กล่องข้อความ | กล่องแจ้งเตือน |

### Input Fields (คำถาม)
| Type | คำถามแบบ |
|------|----------|
| `text` | ข้อความสั้น |
| `textarea` | ข้อความยาว |
| `email` | อีเมล |
| `tel` | เบอร์โทร |
| `dropdown` | Dropdown |
| `choice` | ตัวเลือกเดียว |
| `multiple_choice` | หลายตัวเลือก |
| `rating` | ให้คะแนน |
| `date` | วันที่ |

---

## 📐 Concept: Card Based Layout

```
┌─────────────────────────────────────┐
│  [HEADING] แบบสอบถามความพึงพอใจ     │  ← heading
├─────────────────────────────────────┤
│                                     │
│  📋 ข้อมูลส่วนตัว                   │  ← section (เริ่ม Card 1)
│  ─────────────────────              │
│  ชื่อ: [_________]                  │  ← text
│  อีเมล: [_________]                 │  ← email
│  จังหวัด: [▼______]                │  ← dropdown
│                                     │
├─────────────────────────────────────┤
│                                     │
│  🛎️ ข้อมูลบริการ                    │  ← section (เริ่ม Card 2)
│  ─────────────────────              │
│  บริการ: [▼______]                 │  ← dropdown
│  คะแนน: ⭐⭐⭐⭐⭐                    │  ← rating
│                                     │
├─────────────────────────────────────┤
│                                     │
│  💬 ข้อเสนอแนะ                      │  ← section (เริ่ม Card 3)
│  ─────────────────────              │
│  [ข้อเสนอแนะ...     ]              │  ← textarea
│                                     │
└─────────────────────────────────────┘
```

**หลักการ:** Fields ที่อยู่ระหว่าง `section` ถูกจัดกลุ่มเข้าด้วยกัน

---

## 🛠️ วิธีสร้าง Form แบบ Card Based

### Step 1: เพิ่ม Heading (หัวข้อใหญ่)
```
เลือก: "หัวข้อ" (heading)
ข้อความ: "แบบสอบถามความพึงพอใจ"
```

### Step 2: เพิ่ม Section แรก
```
เลือก: "Section" (section)
ชื่อ: "📋 ข้อมูลส่วนตัว"
```

### Step 3: เพิ่มคำถามใน Section นั้น
```
เลือก: "ข้อความสั้น" (text)
คำถาม: "ชื่อ-นามสกุล"
บังคับตอบ: ✅

เลือก: "Dropdown" (dropdown)
คำถาม: "จังหวัด"
ตัวเลือก: กรุงเทพฯ, เชียงใหม่, ภูเก็ต
```

### Step 4: เพิ่ม Section ถัดไป
```
เลือก: "Section" (section)
ชื่อ: "🛎️ ข้อมูลบริการ"
```

### Step 5: เพิ่มคำถามใน Section 2
```
เลือก: "Dropdown" (dropdown)
คำถาม: "ประเภทบริการ"

เลือก: "ให้คะแนน" (rating)
คำถาม: "ความพึงพอใจ"
```

---

## 💻 Code: วิธี Render Card Based

```tsx
// FormRenderer แบบ Card Based
function CardBasedRenderer({ fields, formData }) {
  // แบ่ง fields เป็นกลุ่มตาม section
  const sections = [];
  let currentSection = { title: null, fields: [] };
  
  fields.forEach(field => {
    if (field.type === 'section') {
      // ถ้าเจอ section ใหม่ ให้เริ่มกลุ่มใหม่
      if (currentSection.fields.length > 0 || currentSection.title) {
        sections.push(currentSection);
      }
      currentSection = { 
        title: field.label, 
        icon: getSectionIcon(field.label),
        fields: [] 
      };
    } else if (field.type === 'heading') {
      // heading แยกเก็บไว้
      sections.push({ type: 'heading', content: field.label });
    } else {
      // field ปกติใส่ใน section ปัจจุบัน
      currentSection.fields.push(field);
    }
  });
  
  // ใส่ section สุดท้าย
  if (currentSection.fields.length > 0 || currentSection.title) {
    sections.push(currentSection);
  }
  
  return (
    <div className="max-w-2xl mx-auto space-y-6">
      {/* Header */}
      {formData.logo_url && (
        <img src={formData.logo_url} className="w-16 h-16 mx-auto" />
      )}
      <h1 className="text-2xl font-bold text-center">{formData.title}</h1>
      
      {/* Render Sections */}
      {sections.map((section, idx) => {
        if (section.type === 'heading') {
          return (
            <h2 key={idx} className="text-xl font-bold text-center">
              {section.content}
            </h2>
          );
        }
        
        return (
          <Card key={idx} title={section.title} icon={section.icon}>
            {section.fields.map(field => (
              <FieldRenderer key={field.id} field={field} />
            ))}
          </Card>
        );
      })}
    </div>
  );
}

// Card Component
function Card({ title, icon, children }) {
  return (
    <div className="bg-white rounded-2xl shadow-sm border border-slate-200 overflow-hidden">
      {/* Card Header */}
      <div className="bg-emerald-50 px-6 py-4 border-b border-emerald-100">
        <div className="flex items-center gap-3">
          <span className="text-2xl">{icon}</span>
          <h3 className="font-bold text-emerald-900">{title}</h3>
        </div>
      </div>
      
      {/* Card Content */}
      <div className="p-6 space-y-5">
        {children}
      </div>
    </div>
  );
}
```

---

## 🎨 Theme Variants สำหรับ Card Based

### Theme A: Card แยกกัน (Default)
```
┌─────────────┐
│ Card 1      │
├─────────────┤
│ คำถาม...    │
└─────────────┘

┌─────────────┐
│ Card 2      │
├─────────────┤
│ คำถาม...    │
└─────────────┘
```

### Theme B: Card ติดกัน (Connected)
```
┌─────────────┐
│ Card 1      │
├─────────────┤
│ คำถาม...    │
├─────────────┤  ← ไม่มีช่องว่าง
│ Card 2      │
├─────────────┤
│ คำถาม...    │
└─────────────┘
```

### Theme C: Card ซ้อนในกล่องใหญ่
```
┌─────────────────────────┐
│  กล่องใหญ่ (Container)  │
│  ┌─────────────────┐    │
│  │ Section 1       │    │
│  │ ─────────────   │    │
│  │ คำถาม...        │    │
│  └─────────────────┘    │
│  ┌─────────────────┐    │
│  │ Section 2       │    │
│  │ ─────────────   │    │
│  │ คำถาม...        │    │
│  └─────────────────┘    │
└─────────────────────────┘
```

---

## 📊 เปรียบเทียบ: ใช้ Section vs ไม่ใช้

### ❌ ไม่ใช้ Section (Form ธรรมดา)
```
แบบสอบถามความพึงพอใจ

1. ชื่อ: [_________]
2. อีเมล: [_________]
3. จังหวัด: [▼_____]
4. บริการ: [▼_____]
5. คะแนน: [⭐⭐⭐]
6. ข้อเสนอแนะ: [___]

[ส่งคำตอบ]
```
**ปัญหา:** คำถามเยอะ ดูยาก ไม่เป็นระเบียบ

### ✅ ใช้ Section (Card Based)
```
แบบสอบถามความพึงพอใจ

┌─────────────────┐
│ 📋 ข้อมูลส่วนตัว │
├─────────────────┤
│ ชื่อ: [_______] │
│ อีเมล: [______] │
│ จังหวัด: [▼___] │
└─────────────────┘

┌─────────────────┐
│ 🛎️ ข้อมูลบริการ │
├─────────────────┤
│ บริการ: [▼____] │
│ คะแนน: [⭐⭐⭐] │
└─────────────────┘

┌─────────────────┐
│ 💬 ข้อเสนอแนะ   │
├─────────────────┤
│ [             ] │
│ [             ] │
└─────────────────┘

[ส่งคำตอบ]
```
**ดีกว่า:** จัดกลุ่มชัดเจน อ่านง่าย ไม่ overwhelm

---

## 🎯 Best Practices

### 1. เริ่มต้นด้วย Heading
```
[Heading] แบบสอบถามความพึงพอใจลูกค้า
```

### 2. แบ่ง Section ตามหัวข้อ
- 📋 ข้อมูลส่วนตัว (3-4 คำถาม)
- 🛎️ ข้อมูลบริการ (2-3 คำถาม)
- 💬 ความคิดเห็น (1-2 คำถาม)

### 3. ไม่ต้อง Section ทุกคำถาม
```
❌ ไม่ดี:
[Section] ชื่อ
[Question] ชื่อ?
[Section] อีเมล
[Question] อีเมล?

✅ ดี:
[Section] ข้อมูลติดต่อ
[Question] ชื่อ?
[Question] อีเมล?
```

### 4. ใช้ Icon ใน Section Title
```
📋 ข้อมูลส่วนตัว
🛎️ ข้อมูลบริการ  
💬 ความคิดเห็น
⭐ การประเมิน
📍 ตำแหน่ง
```

---

## 🔧 การใช้งานจริงใน Form Builder

### 1. เปิด Form Builder
```
/admin/forms/create หรือ /admin/forms/[id]/edit
```

### 2. สร้างโครงสร้าง
```
[+] หัวข้อ (heading)
   → "แบบสอบถามความพึงพอใจ"

[+] Section (section)  
   → "📋 ข้อมูลส่วนตัว"

[+] ข้อความสั้น (text)
   → "ชื่อ-นามสกุล" (บังคับ)

[+] Dropdown (dropdown)
   → "จังหวัด"
   → เพิ่มตัวเลือก: กรุงเทพฯ, เชียงใหม่, ภูเก็ต

[+] Section (section)
   → "🛎️ ข้อมูลบริการ"

[+] Dropdown (dropdown)
   → "ประเภทบริการ"

[+] ให้คะแนน (rating)
   → "ความพึงพอใจโดยรวม"
```

### 3. ลากเรียงลำดับ
ใช้ drag & drop จัดเรียง fields ได้ตามต้องการ

---

## 💡 ฟีเจอร์เพิ่มเติมที่ควรมี

### 1. Section Settings
```typescript
interface SectionSettings {
  icon?: string;           // Emoji icon
  color?: 'blue' | 'green' | 'purple';  // สี header
  description?: string;    // คำอธิบายใต้ชื่อ section
  collapsible?: boolean;   // พับ/กางได้
}
```

### 2. Conditional Sections
```
[Section] ข้อมูลบริการออนไลน์
แสดงเมื่อ: ประเภทบริการ = "ออนไลน์"
```

### 3. Progress Bar
```
[==============>    ] 60%
Section 2 จาก 3
```

---

## 🚀 สรุป

| คำถาม | คำตอบ |
|-------|--------|
| แบ่ง Section ยังไง? | ใช้ `section` field type |
| ได้ Card Based ไหม? | ได้ โดย render section เป็น card |
| ต้องแก้โค้ดไหม? | แค่ update FormRenderer |
| Dropdown ใช้ได้ไหม? | ได้ มี `dropdown` field type |

**ต่อไป:** ต้องการให้ implement Card Based FormRenderer ใช่ไหมครับ?
