# คู่มือเชื่อมต่อ Supabase ฉบับสมบูรณ์

## ภาพรวม

```
┌─────────────────┐         ┌──────────────────┐         ┌─────────────────┐
│   Next.js App   │────────▶│    Supabase      │────────▶│   PostgreSQL    │
│   (localhost)   │  HTTPS  │   (supabase.co)  │  SQL    │   Database      │
└─────────────────┘         └──────────────────┘         └─────────────────┘
       │                             │
       │ 1. สร้าง Project            │ 2. รัน SQL Migrations
       │ 3. ตั้งค่า ENV              │ 4. เชื่อมต่อ
       ▼                             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         ขั้นตอนที่ต้องทำ                               │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Step 1: สร้าง Supabase Project

### 1.1 สมัครและสร้าง Project

1. ไปที่ https://supabase.com
2. คลิก "Start your project" → Sign up (ใช้ GitHub หรือ Email)
3. คลิก "New Project"
4. ตั้งค่า:
   - **Organization**: เลือกหรือสร้างใหม่
   - **Project name**: `questionnaire-qr` (หรือชื่อที่ต้องการ)
   - **Database Password**: ตั้งรหัสผ่านที่แข็งแรง (เก็บไว้ให้ดี!)
   - **Region**: เลือก `Singapore` หรือ `Tokyo` (ใกล้ไทยที่สุด)
5. คลิก "Create new project"
6. รอ 2-3 นาทีให้สร้างเสร็จ

### 1.2 เก็บ Credentials สำคัญ

พอ project พร้อม ให้ไปที่ **Project Settings** (ไอคอนเกียร์) → **API**

จะเห็นข้อมูลนี้ (ก็อปเก็บไว้):

```
📋 Project URL: https://xxxxxxxx.supabase.co
📋 anon public: eyJhbGciOiJIUzI1NiIs... (อย่าโพสต์สาธารณะ!)
📋 service_role: eyJhbGciOiJIUzI1NiIs... (ห้ามหลุดเด็ดขาด!)
```

---

## Step 2: ตั้งค่า Environment Variables

### 2.1 สร้างไฟล์ `.env.local`

ในโฟลเดอร์ `my-app` สร้างไฟล์ `.env.local`:

```bash
cd my-app
touch .env.local
```

### 2.2 ใส่ค่าที่ก็อปมา

```env
# ============================================
# Supabase Configuration (จำเป็นต้องมี)
# ============================================

# Project URL (ตัวอย่าง: https://abc123.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co

# Anon Key (ใช้ใน browser - ปลอดภัยที่จะ expose)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...

# Service Role Key (ใช้บน server เท่านั้น - ห้าม expose!)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIs...

# ============================================
# App Configuration
# ============================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

⚠️ **สำคัญ:**
- `NEXT_PUBLIC_` = ใช้ใน browser ได้ (แต่มีสิทธิ์จำกัด)
- `SUPABASE_SERVICE_ROLE_KEY` = ใช้แค่ server-side เท่านั้น (มีสิทธิ์เต็ม)

---

## Step 3: รัน SQL Migrations

### 3.1 เปิด SQL Editor

ใน Supabase Dashboard:
1. คลิก **SQL Editor** (เมนูซ้าย)
2. คลิก **New query**

### 3.2 รัน Migration แรก (สร้างตารางหลัก)

วางโค้ดจากไฟล์ `supabase/migrations/001_initial_schema.sql` ทั้งหมด:

```sql
-- สร้างตารางหลักทั้งหมด
-- 1. projects
-- 2. forms  
-- 3. qr_codes
-- 4. submissions
-- + Views และ Functions
```

คลิก **Run** หรือกด `Ctrl + Enter`

### 3.3 รัน Migration ที่ 2 (Consent + Form Version)

วางโค้ดจาก `supabase/migrations/002_add_consent_fields.sql`:

```sql
-- เพิ่มฟิลด์ consent
-- + form_versions table
```

### 3.4 รัน Migration ที่ 3 (Form Version System)

วางโค้ดจาก `supabase/migrations/003_form_version_system.sql`:

```sql
-- เพิ่มฟิลด์ status, current_version
-- + Functions publish_form(), create_new_version()
```

✅ **ตรวจสอบ:** ไปที่ **Table Editor** ควรเห็นตารางทั้งหมด:
- `forms`
- `qr_codes`
- `submissions`
- `projects`
- `form_versions`

---

## Step 4: ติดตั้ง Dependencies

```bash
cd my-app

# ติดตั้ง Supabase client
npm install @supabase/supabase-js @supabase/ssr

# ติดตั้ง dependencies อื่นๆ (ถ้ายังไม่มี)
npm install lucide-react xlsx
```

---

## Step 5: ทดสอบการเชื่อมต่อ

### 5.1 รันโปรเจค

```bash
npm run dev
```

### 5.2 ทดสอบหน้า Admin

ไปที่ http://localhost:3000/admin

ควรเห็น:
- ✅ Dashboard โหลดข้อมูล (ไม่ error)
- ✅ สร้างฟอร์มได้
- ✅ QR Code แสดงผล

### 5.3 ทดสอบการบันทึกข้อมูล

1. สร้างฟอร์มใหม่
2. กด "Publish"
3. ไปดูที่ Supabase → Table Editor → `forms`
4. ควรเห็นข้อมูลที่สร้าง

---

## โครงสร้างการเชื่อมต่อในโค้ด

### Client-side (Browser)

```typescript
// src/lib/supabase.ts
import { createBrowserClient } from '@supabase/ssr';

export const createClient = () => {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  );
};
```

### ใช้งานใน Component

```typescript
// src/hooks/useSupabase.ts
import { getSupabaseBrowser } from '@/lib/supabase';

export function useForms() {
  const fetchForms = async () => {
    const supabase = getSupabaseBrowser();
    const { data, error } = await supabase
      .from('forms')
      .select('*')
      .order('created_at', { ascending: false });
    
    if (error) throw error;
    return data;
  };
  
  // ...
}
```

---

## การทำงานของแต่ละตาราง

### forms (แบบสอบถาม)
```sql
- เก็บข้อมูลฟอร์ม + fields (JSONB)
- current_version: version ล่าสุด
- status: draft/published/archived
```

### form_versions (ประวัติ version)
```sql
- เก็บ snapshot ของ fields แต่ละ version
- เชื่อมกับ submissions ผ่าน form_version
```

### qr_codes
```sql
- ผูกกับ form_id
- qr_slug: ใช้สร้าง URL (/qr/xxx)
- utm_*: สำหรับ track แหล่งที่มา
- scan_count: จำนวนสแกน
```

### submissions (คำตอบ)
```sql
- responses: JSONB {field_id: value}
- form_version: รู้ว่าตอบตอนไหน
- consent_*: ข้อมูลการยินยอม
- utm_*: แหล่งที่มาของผู้ตอบ
```

---

## แก้ไขปัญหาเบื้องต้น

### ❌ Error: Missing Supabase environment variables
```
แก้: ตรวจสอบว่า .env.local มีค่าครบและชื่อถูกต้อง
```

### ❌ Error: Cannot connect to Supabase
```
แก้: 
1. ตรวจสอบ internet
2. ตรวจสอบ Project URL ถูกต้องไหม
3. ลองรีสตาร์ท dev server (Ctrl+C แล้ว npm run dev)
```

### ❌ Error: Permission denied
```
แก้: 
1. ไปที่ Supabase → Authentication → Policies
2. ตรวจสอบ RLS policies ถูกต้อง
3. หรือปิด RLS ชั่วคราวเพื่อทดสอบ (ไม่แนะนำใน production)
```

### ❌ Error: Table does not exist
```
แก้: รัน SQL migrations ให้ครบทุกไฟล์
```

---

## สรุปสิ่งที่เชื่อมต่อแล้ว

| Feature | เชื่อม Supabase | ตารางที่ใช้ |
|---------|----------------|------------|
| Dashboard | ✅ | forms, qr_codes, submissions |
| Forms | ✅ | forms, form_versions |
| QR Codes | ✅ | qr_codes |
| Projects | ✅ | projects |
| Analytics | ✅ | submissions + views |
| Form Submit | ✅ | submissions |
| Consent | ✅ | submissions (consent_*) |

---

## ขั้นตอนสรุป (Checklist)

- [ ] สร้าง Supabase Project
- [ ] เก็บ URL และ Keys
- [ ] สร้าง `.env.local` ในโฟลเดอร์ my-app
- [ ] รัน Migration 001 (ตารางหลัก)
- [ ] รัน Migration 002 (Consent)
- [ ] รัน Migration 003 (Form Version)
- [ ] ติดตั้ง dependencies
- [ ] รัน `npm run dev`
- [ ] ทดสอบสร้างฟอร์ม
- [ ] ตรวจสอบข้อมูลในตาราง

---

**ติดปัญหาตรงไหนถามได้เลยครับ!** 🚀
