# ==============================================================================
# Supabase Configuration (ตั้งค่าฐานข้อมูล Supabase)
# ==============================================================================

# URL ของ API ฐานข้อมูล Supabase (ใช้ร่วมกันทั้ง Frontend และ Backend)
# หาได้จาก: Supabase Dashboard -> Project Settings -> API -> Project URL
# ตัวอย่าง: https://abcdefghijklmnopqrstuvwxyz.supabase.co
NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_URL

# API Key สาธารณะ (Anon Key)
# ใช้สำหรับเชื่อมต่อจากฝั่ง Frontend (Next.js/React)
# ความปลอดภัย: ปลอดภัยที่จะเปิดเผยใน Browser, สิทธิ์การเข้าถึงถูกจำกัดด้วย RLS (Row Level Security) ใน Database
# หาได้จาก: Supabase Dashboard -> Project Settings -> API -> anon public
# ตัวอย่าง: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY

# API Key ลับสำหรับ Backend (Service Role Key)
# ⚠️ สำคัญมาก: ห้ามเปิดเผยให้ User เห็นเด็ดขาด (ห้ามใช้ใน Frontend)
# ใช้สำหรับ Backend เท่านั้น, มีสิทธิ์เข้าถึงทุกอย่างใน Database (ข้าม RLS ทั้งหมด)
# หาได้จาก: Supabase Dashboard -> Project Settings -> API -> service_role secret
# ตัวอย่าง: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# ==============================================================================
# Application Configuration (ตั้งค่าแอปพลิเคชัน)
# ==============================================================================

# URL หลักของเว็บไซต์
# ใช้สำหรับสร้าง Link หรือ Redirect ต่างๆ (เช่น เวลากด Login, หรือ QR Code)
# - ช่วง Dev: ใช้ http://localhost:3000 หรือ Port ที่รันอยู่
# - ช่วง Production: ใช้ URL จริงของเว็บ (เช่น https://my-app.vercel.app)
NEXT_PUBLIC_APP_URL=http://localhost:3000
